{
    "scripts": {
        "build": "esbuild js/widget.js --bundle --format=esm --outdir=src/graphviz_anywidget/static --loader:.wasm=binary",
        "dev": "esbuild js/widget.js --bundle --format=esm --outdir=src/graphviz_anywidget/static --loader:.wasm=binary --sourcemap=inline --watch"
    },
    "dependencies": {
        "d3": "7.9.0",
        "d3-graphviz": "5.6.0",
        "graphvizsvg": "1.0.1"
    },
    "devDependencies": {
        "esbuild": "^0.24.0"
    }
}
